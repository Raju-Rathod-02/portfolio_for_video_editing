# Environment Configuration
# Copy this to .env and update values for production

NODE_ENV=development
PORT=3000

# Database (for future backend integration)
# DB_HOST=localhost
# DB_USER=admin
# DB_PASS=password
# DB_NAME=raju_db

# Email (for contact form if integrating with backend)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Admin credentials (optional - for backend authentication)
# ADMIN_EMAIL=admin@raju.com
# ADMIN_PASSWORD_HASH=will_be_hashed_automatically
